# Python dependencies for the interactive correction collection workflow.
# OpenPI itself is a separate source checkout; set OPENPI_ROOT to that checkout.
# Python 3.11 is recommended because the OpenPI runtime expects it.

# Core runtime
numpy
packaging>=24.2
Pillow>=10.0.0
opencv-python-headless>=4.9.0,<4.13.0
# Optional for --gui-backend opencv on a local desktop:
# opencv-python>=4.9.0
av>=12.0.5
imageio[ffmpeg]>=2.34.0
jsonlines>=4.0.0
safetensors

# Torch / policy runtime
torch>=2.2.1,<2.8.0
torchvision>=0.21.0,<0.23.0
einops>=0.8.0
diffusers>=0.27.2

# LeRobot dataset/runtime helpers
datasets>=2.19.0
huggingface-hub[hf-transfer,cli]>=0.27.1
deepdiff>=7.0.1
draccus>=0.10.0
hydra-core>=1.3.2
omegaconf>=2.3.0
rerun-sdk>=0.21.0
termcolor>=2.4.0
zarr>=2.17.0
h5py>=3.10.0
numba>=0.59.0
pynput>=1.7.7
pyzmq>=26.2.1
wandb>=0.16.3

# Robot / camera hardware
piper_sdk>=0.6.1,<0.7.0
python-can>=4.0.0
pyserial>=3.5
pyrealsense2>=2.55.1.6486,<2.57.0; sys_platform != "darwin"
pyrealsense2-macosx>=2.54,<2.55.0; sys_platform == "darwin"
